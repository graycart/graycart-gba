<!--
Cited: gbadev-org/libtonc-examples (CC0-1.0)
Note: ≥3 demo goldens required for G4-tonc; demos not vendored yet (no prebuilt
.gba in upstream tree without devkitARM). Gate stays #[ignore] until demos land.
-->
# Tonc goldens (P4)

Placeholder directory for `*.hash` files matching `tests/fixtures/tonc/*.gba`.

Same hash format as jsmolka-ppu (`frame=` + `sha256=`).
