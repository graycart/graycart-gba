//! DMG/CGB-via-GBA hardware compat placeholder (P10+).
//!
//! Module layout from graycart-gba implementation plan §2.2 / §3.11.
//! Prefer graycart-gb / gb-core reuse — do not reinvent SM83/PPU/APU here.

/// Stub compat wrapper (empty until P10).
#[derive(Debug, Default)]
pub struct Compat;
